! ============================================================
! Device: SW3 — Branch Access Switch
! Role: Branch end device connectivity
! ============================================================

hostname SW3_BRANCH_ACCESS_SWITCH

! ── SSH ──────────────────────────────────────────────────────
ip domain-name corp.lab
ip ssh version 2
username admin secret Admin@123
crypto key generate rsa modulus 2048

! ── DHCP Snooping ────────────────────────────────────────────
ip dhcp snooping
ip dhcp snooping vlan 10,20
no ip dhcp snooping information option

! ── STP ──────────────────────────────────────────────────────
spanning-tree mode rapid-pvst

! ── Interfaces ───────────────────────────────────────────────
! Trunk toward R2 — trusted for DHCP snooping
interface FastEthernet0/1
 switchport mode trunk
 ip dhcp snooping trust

! PC-Branch1 — VLAN 10
interface FastEthernet0/2
 switchport mode access
 switchport access vlan 10
 description PC-BRANCH1
 switchport port-security
 switchport port-security maximum 1
 switchport port-security mac-address sticky
 switchport port-security violation restrict
 ip dhcp snooping limit rate 15
 spanning-tree portfast

! PC-Branch2 — VLAN 20
interface FastEthernet0/3
 switchport mode access
 switchport access vlan 20
 description PC-BRANCH2
 switchport port-security
 switchport port-security maximum 1
 switchport port-security mac-address sticky
 switchport port-security violation restrict
 ip dhcp snooping limit rate 15
 spanning-tree portfast

! ── Management Interface ──────────────────────────────────────
interface Vlan20
 ip address 192.168.120.3 255.255.255.240
 no shutdown

ip default-gateway 192.168.120.1

! ── VLANs ────────────────────────────────────────────────────
vlan 10
 name Branch1
vlan 20
 name Branch2

! ── Line Configuration ────────────────────────────────────────
line con 0
 password Cisco@123
 login

line vty 0 4
 login local
 transport input ssh
