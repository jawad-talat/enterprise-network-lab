! ============================================================
! Device: SW2 — HQ Access Switch
! Role: End device connectivity, VLAN access ports
! ============================================================

hostname SW2_HQ_ACCESS_SWITCH

! ── SSH ──────────────────────────────────────────────────────
ip domain-name corp.lab
ip ssh version 2
username admin secret Admin@123
crypto key generate rsa modulus 2048

! ── DHCP Snooping ────────────────────────────────────────────
ip dhcp snooping
ip dhcp snooping vlan 10-40
no ip dhcp snooping information option

! ── STP ──────────────────────────────────────────────────────
spanning-tree mode rapid-pvst

! ── Interfaces ───────────────────────────────────────────────
! Trunk toward SW1 — trusted for DHCP snooping
interface FastEthernet0/1
 switchport mode trunk
 ip dhcp snooping trust

! PC-Sales — VLAN 10
interface FastEthernet0/2
 switchport mode access
 switchport access vlan 10
 description PC-Sales
 switchport port-security
 switchport port-security maximum 1
 switchport port-security mac-address sticky
 switchport port-security violation restrict
 ip dhcp snooping limit rate 15
 spanning-tree portfast

! PC-IT — VLAN 20
interface FastEthernet0/3
 switchport mode access
 switchport access vlan 20
 description PC-IT
 switchport port-security
 switchport port-security maximum 1
 switchport port-security mac-address sticky
 switchport port-security violation restrict
 ip dhcp snooping limit rate 15
 spanning-tree portfast

! PC-HR — VLAN 30
interface FastEthernet0/4
 switchport mode access
 switchport access vlan 30
 description PC-HR
 switchport port-security
 switchport port-security maximum 1
 switchport port-security mac-address sticky
 switchport port-security violation restrict
 ip dhcp snooping limit rate 15
 spanning-tree portfast

! PC-Management — VLAN 40
interface FastEthernet0/5
 switchport mode access
 switchport access vlan 40
 description PC-MANAGEMENT
 switchport port-security
 switchport port-security maximum 1
 switchport port-security mac-address sticky
 switchport port-security violation restrict
 ip dhcp snooping limit rate 15
 spanning-tree portfast

! ── Management Interface ──────────────────────────────────────
interface Vlan40
 ip address 192.168.40.5 255.255.255.248
 no shutdown

ip default-gateway 192.168.40.1

! ── VLANs ────────────────────────────────────────────────────
vlan 10
 name Sales
vlan 20
 name IT
vlan 30
 name HR
vlan 40
 name Management

! ── Line Configuration ────────────────────────────────────────
line con 0
 password Cisco@123
 login

line vty 0 4
 login local
 transport input ssh
